<panels:PropertyPanel x:Class="Macad.Interaction.Editors.Shapes.HelixPropertyPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:mmp="clr-namespace:Macad.Presentation;assembly=Macad.Presentation"
             xmlns:mmi="clr-namespace:Macad.Interaction"
             xmlns:panels="clr-namespace:Macad.Interaction.Panels"
             mc:Ignorable="d"
             d:DesignWidth="250"
             DataContext="{Binding RelativeSource={RelativeSource Self}}"
             Style="{DynamicResource Macad.Styles.PropertyPanel}"
             Header="Helix">
    
    <Grid Margin="10,5,0,5">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="80" />
            <ColumnDefinition />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Pitch -->
        <TextBlock Grid.Row="0" Grid.Column="0" Text="Pitch" Style="{DynamicResource Macad.Styles.TextBlock.Property}" />
        <mmp:ValueEditBox Grid.Row="0" Grid.Column="1" Margin="0,2"
                          Units="Length" Value="{Binding Helix.Pitch, NotifyOnSourceUpdated=True}" />

        <!-- Height -->
        <TextBlock Grid.Row="1" Grid.Column="0" Text="Height" Style="{DynamicResource Macad.Styles.TextBlock.Property}" />
        <mmp:ValueEditBox Grid.Row="1" Grid.Column="1" Margin="0,2"
                          Units="Length" Value="{Binding Helix.Height, NotifyOnSourceUpdated=True}" />

        <!-- Radius -->
        <TextBlock Grid.Row="2" Grid.Column="0" Text="Radius" Style="{DynamicResource Macad.Styles.TextBlock.Property}" />
        <mmp:ValueEditBox Grid.Row="2" Grid.Column="1" Margin="0,2"
                          Units="Length" Value="{Binding Helix.Radius, NotifyOnSourceUpdated=True}" />

        <!-- Taper Angle -->
        <TextBlock Grid.Row="3" Grid.Column="0" Text="Taper Angle" Style="{DynamicResource Macad.Styles.TextBlock.Property}" />
        <mmp:ValueEditBox Grid.Row="3" Grid.Column="1" Margin="0,2"
                          Units="Degree" Value="{Binding Helix.TaperAngle, NotifyOnSourceUpdated=True}" 
                          MinValue="-89.9" MaxValue="89.9" />

        <!-- Handedness / Direction -->
        <TextBlock Grid.Row="4" Grid.Column="0" Text="Direction" Style="{DynamicResource Macad.Styles.TextBlock.Property}" />
        <StackPanel Grid.Row="4" Grid.Column="1" Margin="0,4" Orientation="Horizontal">
            <mmp:ToggleButton Content="Right" mmp:GroupControls.Position="Left" Width="60"
                              IsChecked="{Binding Helix.Handedness, Converter={mmp:EqualityToBoolConverter}, ConverterParameter=Right, Mode=OneWay}"
                              Command="{Binding SwitchHandednessCommand}" CommandParameter="Right"/>
            <mmp:ToggleButton Content="Left" mmp:GroupControls.Position="Right" Width="60"
                              IsChecked="{Binding Helix.Handedness, Converter={mmp:EqualityToBoolConverter}, ConverterParameter=Left, Mode=OneWay}"
                              Command="{Binding SwitchHandednessCommand}" CommandParameter="Left"/>
        </StackPanel>
    </Grid>
</panels:PropertyPanel>
