<panels:PropertyPanel x:Class="Macad.Interaction.Editors.Auxiliary.DatumLinePropertyPanel"
                      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
                      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
                      xmlns:panels="clr-namespace:Macad.Interaction.Panels"
                      xmlns:mmp="clr-namespace:Macad.Presentation;assembly=Macad.Presentation"
                      xmlns:local="clr-namespace:Macad.Interaction.Editors.Auxiliary"
                      mc:Ignorable="d" 
                      DataContext="{Binding RelativeSource={RelativeSource Self}}" 
                      Style="{DynamicResource Macad.Styles.PropertyPanel}"
                      Header="Datum Line">

    <StackPanel>
        <!-- Interactive -->
        <panels:InteractivePropertyGroup Entity="{Binding DatumLine}" />

        <!-- Transformable -->
        <panels:TransformablePropertyGroup Transformable="{Binding DatumLine}" />

        <Grid Margin="0,20,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="80" />
                <ColumnDefinition />
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <!-- Length -->
            <TextBlock Grid.Row="0" Grid.Column="0" 
                       Text="Length" 
                       Style="{StaticResource Macad.Styles.TextBlock.Property}" />
            <mmp:ValueEditBox Grid.Row="0" Grid.Column="1"
                              Units="Length" MinValue="0.01"
                              Value="{Binding DatumLine.Length, NotifyOnSourceUpdated=True}"
                              AutomationProperties.AutomationId="Length"/>
        </Grid>
    </StackPanel>

</panels:PropertyPanel>
