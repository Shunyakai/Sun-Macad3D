<mmp:Dialog x:Class="Macad.Interaction.Dialogs.CenterViewDialog"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:mmp="clr-namespace:Macad.Presentation;assembly=Macad.Presentation"
             mc:Ignorable="d" 
             Title="Center View"
             ResizeMode="NoResize" 
             ShowInTaskbar="False" 
             Width="300" Height="220"
             Style="{StaticResource {x:Type mmp:Dialog}}"
             DataContext="{Binding RelativeSource={RelativeSource Self}}">
    
    <mmp:Dialog.Footer>
        <DockPanel LastChildFill="False">
            <Button DockPanel.Dock="Right" Content="Cancel" IsCancel="True" Click="Cancel_Click"
                    Style="{StaticResource Macad.Styles.Button.DialogFooter}" />
            <Button DockPanel.Dock="Right" Content="Center" IsDefault="True" Click="Center_Click"
                    Style="{StaticResource Macad.Styles.Button.DialogFooter}" />
        </DockPanel>
    </mmp:Dialog.Footer>

    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Enter target coordinates to center the view:" Margin="0,0,0,15" TextWrapping="Wrap"/>

        <Grid Grid.Row="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="5"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="5"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="50"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <TextBlock Grid.Row="0" Grid.Column="0" Text="X:" VerticalAlignment="Center"/>
            <mmp:ValueEditBox Grid.Row="0" Grid.Column="1" Value="{Binding X, Mode=TwoWay}" Units="Length"/>

            <TextBlock Grid.Row="2" Grid.Column="0" Text="Y:" VerticalAlignment="Center"/>
            <mmp:ValueEditBox Grid.Row="2" Grid.Column="1" Value="{Binding Y, Mode=TwoWay}" Units="Length"/>

            <TextBlock Grid.Row="4" Grid.Column="0" Text="Z:" VerticalAlignment="Center"/>
            <mmp:ValueEditBox Grid.Row="4" Grid.Column="1" Value="{Binding Z, Mode=TwoWay}" Units="Length"/>
        </Grid>
    </Grid>
</mmp:Dialog>
